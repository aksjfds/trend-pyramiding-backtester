# Copy this file to okx.credentials.toml in the same directory and fill it locally.
# The real credentials file is ignored by Git. Keep its permissions at 600.
# Filled values take priority over environment variables and legacy JSON files.

[live]
api_key = ""
api_secret = ""
passphrase = ""

# Only used with --demo; never copy production keys into this section.
[demo]
api_key = ""
api_secret = ""
passphrase = ""
